<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.windvolt"
    android:versionCode="202110"
    android:versionName="V2021.10">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".diagram.Fotos"></activity>
        <activity android:name=".diagram.News" />
        <activity android:name=".story.StoryOfWindvolt" />
        <activity android:name=".diagram.Gallery" />
        <activity android:name=".diagram.Economy" />
        <activity android:name=".diagram.Personal" />
        <activity android:name=".diagram.Devices" />
        <activity android:name=".system.About" />
        <activity android:name=".system.Settings" />
        <activity
            android:name=".AppActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>